bootstrap - files to build the initial oberon compiler for osys

Uses the modified patchouli compiler from smoc/source to build the
osys Oberon compiler using source from this directory (stubs for
Display, Fonts and Oberon), the osys directory (OR*.mod etc.), and
the smoc source directory (boot, heap, kernel etc.)
